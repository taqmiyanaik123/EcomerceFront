import React,{ useContext, userContext} from 'react'

const Eith = ()=> {
    const [darkmode,setDarkmode] = userContext(useContext)

return(
    <div style = {
        {backgroundColor: darkmode ? }
        <h2>Current Theme</h2>
    </div>


)


